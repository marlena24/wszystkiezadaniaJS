var a = 2;
var b = 4;

if ((a * a) - (2 * a * b) + (b * b) < 0)
	value = Ujemna;
	
else if ((a * a) - (2 * a * b) + (b * b) > 0)
	value = Dodatnia;
	
else ((a * a) - (2 * a * b) + (b * b) === 0)
	value = 0;